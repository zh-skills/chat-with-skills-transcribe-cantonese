[project]
name = "chat-with-skills-transcribe-cantonese"
version = "1.0.0"
requires-python = "==3.11.*"
dependencies = [
    "python-dotenv",
    "requests",
    "flask",
    "flask-cors",
    "waitress",
    "llama-cpp-python",
    "faster-whisper",
]
